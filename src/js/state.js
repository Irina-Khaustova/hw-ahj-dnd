export default class State {
  constructor() {
    this.storage = localStorage;
  }

  save() {
    //let card = JSON.parse(this.storage.getItem('cardList'));
    if (null === null) {
      let card = {
        todo: [],
        inProgress: [],
        done: [],
      };
    }

   // if ()
   // console.log(card);
  //}

}
}